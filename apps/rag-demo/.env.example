# Core answer-provider settings.
# Leave provider/model empty for demo mode (citations-only, no external LLM calls).
N3_ANSWER_PROVIDER=
N3_ANSWER_MODEL=

# OpenAI key options (set one when provider=openai).
NAMEL3SS_OPENAI_API_KEY=
OPENAI_API_KEY=

# Anthropic key options (set one when provider=anthropic).
NAMEL3SS_ANTHROPIC_API_KEY=
ANTHROPIC_API_KEY=

# Deterministic local persistence path.
N3_PERSIST_ROOT=./data
